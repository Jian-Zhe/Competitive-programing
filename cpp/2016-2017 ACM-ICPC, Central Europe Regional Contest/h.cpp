#include <bits/stdc++.h>

using namespace std;

int main(void) {
    cin.tie(0) -> sync_with_stdio(0);

    int n;
    cin >> n;

    vector<string> g(n);

    for(int i = 0; i < n; ++i) {
        cin >> g[i];
    }

    
    return 0;
}